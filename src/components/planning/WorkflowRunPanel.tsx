/**
 * WorkflowRunPanel — T1.9 stub.
 * Full implementation deferred to T1.10.
 */

export function WorkflowRunPanel() {
  return <div data-testid="workflow-run-panel">Planning features coming in T1.10</div>
}
