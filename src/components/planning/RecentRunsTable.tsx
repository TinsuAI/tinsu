/**
 * RecentRunsTable — T1.9 stub.
 * Full implementation deferred to T1.10.
 */

export function RecentRunsTable() {
  return <div data-testid="recent-runs-table">Planning features coming in T1.10</div>
}
